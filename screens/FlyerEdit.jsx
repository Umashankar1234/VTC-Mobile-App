import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FlyerEdit = ({ route }) => {
  const { tourid } = route.params;
  return (
    <View>
      <Text>FlyerEdit</Text>
    </View>
  );
};

export default FlyerEdit;

const styles = StyleSheet.create({});
